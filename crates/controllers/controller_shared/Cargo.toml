[package]
name = "controller-shared"
version = "0.1.0"
edition = "2024"

[features]
defmt = ["dep:defmt", "foc/defmt", "embassy-time/defmt"]

[dependencies]
defmt = {version = "1.0.1", optional = true}
embassy-time = {version = "0.5.0"}
foc = {path = "../foc"}
units = { path = "../../utils/units" }