[package]
name = "settings"
version = "0.1.0"
edition = "2024"

[features]
hardware-support = ["dep:hardware"]

[dependencies]
hardware = { path = "../hardware", optional = true, default-features = false }